= render :partial => 'tasks/submit_form'

- if @tasks.present?
  - @tasks.each_slice(3) do |task_row|
    .row
      %ul.thumbnails
        - task_row.each do |task|
          = render :partial => 'tasks/task', :locals => {:task => task}
- else
  .well Your life is spared, use it well!

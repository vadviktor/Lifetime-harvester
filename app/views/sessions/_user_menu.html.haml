- if current_user
  .btn-group
    = link_to '#', {:class => 'btn btn-info dropdown-toggle', "data-toggle" => "dropdown"} do
      = current_user.name
      %span.caret
    %ul.dropdown-menu
      %li= link_to 'Wake me up!', signout_path
